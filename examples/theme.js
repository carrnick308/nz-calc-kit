// Drop-in replacement for each app's src/theme.js
// COLORS + shared constants come from the kit; APP_NAME stays per-app.
export { COLORS, WEBSITE_URL, DISCLAIMER } from 'nz-calc-kit';

export const APP_NAME = 'NZ Rental Property Calculators'; // <-- set per app
